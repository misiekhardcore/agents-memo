name: Formatting

on:
  pull_request:
    branches: [main]

jobs:
  md:
    name: Markdown formatting
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - run: npm run format:md:check

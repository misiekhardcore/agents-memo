// Smoke test placeholder - all tests are in extension-smoke.mjs (node script) and regression scripts.

import { describe, it } from "vitest";

describe("agents-memo smoke", () => {
  it.skip("placeholder — see extension-smoke.mjs for full suite");
});

# First Run Guide

Welcome to your agents-memo vault. Follow these steps to get fully set up.

## 1. Open in Obsidian

1. Open Obsidian
2. **Manage Vaults → Open folder as vault**
3. Select this directory

## 2. Enable Community Plugins

When Obsidian opens, it will warn that community plugins are disabled.

1. Go to **Settings → Community Plugins**
2. Turn off Safe Mode (required for community plugins)

## 3. Install Required Plugins

In **Settings → Community Plugins → Browse**, install:

|Plugin|Purpose|
|-|-|
|**Templater**|Use the templates in `_templates/` to create new pages|

That's it for plugins. The `agents-memo` plugin reaches the vault through the **Obsidian CLI** (shipped with Obsidian 1.12.7+) — no community plugins, TLS bypasses, or API keys required. Just keep Obsidian running with this vault open while you work.

See `_shared/setup.md` for the one-time CLI registration step (`obsidian register vault=...`) and a sanity check.

## 4. Explore the Starter Content

Your vault includes one example page per core type — open them to see the schema in action:

- `wiki/concepts/example-concept.md` — patterns and ideas
- `wiki/entities/example-entity.md` — named real-world things
- `wiki/sources/example-source.md` — ingested document records
- `wiki/questions/example-question.md` — open and answered questions

Structural pages:

- `wiki/hot.md` — recent-context cache (your agent reads this first each session)
- `wiki/index.md` — master page catalog
- `wiki/log.md` — chronological operations log
- `wiki/overview.md` — executive summary of the vault

## 5. Start Using the Wiki

In pi, type `/memo-wiki` to see all available wiki operations, or run the
setup commands directly:

```text
/memo:init             — (re)initialize this vault: bootstrap, git init, vault AGENTS.md, optional lint timer
/memo-wiki query <question>  — ask a question answered from vault content
/memo-wiki ingest <path-or-url> — ingest a document and extract wiki pages
```

This vault also carries an `AGENTS.md` (written by `/memo:init`) so any agent
running inside it knows the wiki conventions without the plugin.

## 6. Optional: Scheduled Linting

Keep the vault healthy automatically. Install the weekly lint timer
(runs the memo-lint skill headless via `pi -p` and commits fixes):

```bash
bin/install-lint-service.sh              # weekly, Sun 03:00
bin/install-lint-service.sh --uninstall  # remove it
```

Installs the `agents-memo-wiki-lint` systemd user timer
(`journalctl --user -u agents-memo-wiki-lint.service` for logs).
Requires Obsidian running at the scheduled time. Run `/memo-lint` interactively
any time for a manual health check.

## 7. Delete the Example Pages (when ready)

Once you understand the schema, delete the example pages:

```text
wiki/concepts/example-concept.md
wiki/entities/example-entity.md
wiki/sources/example-source.md
wiki/questions/example-question.md
```

Keep the structural pages (`hot.md`, `index.md`, `log.md`, `overview.md`).

## Reference: What's Already Configured

### Pre-installed plugins

|Plugin|Purpose|
|-|-|
|Calendar|Sidebar calendar with word count + task dots|
|Thino|Quick memo capture|
|Excalidraw|Freehand drawing + image annotation|
|Banners|Add `banner:` to any note frontmatter for header images|

### CSS snippets

|Snippet|Purpose|
|-|-|
|`vault-colors`|Colour-codes `wiki/` folders in the file explorer|
|`ITS-Image-Adjustments`|Append `\|100` to image embeds for sizing|

### Views

- Graph view — filtered to `wiki/` only, color-coded by page type

### Visual layout (optional)

For a Canvas + Calendar + Thino sidebar layout, quit Obsidian, then run:

```text
cp .obsidian/workspace-visual.json .obsidian/workspace.json
```

Reopen Obsidian.

### Troubleshooting

- **Graph colours reset after closing Obsidian:** open Graph settings → Color groups and re-add them once. They persist permanently after that.

_Generated by agents-memo on first `/memo:init`._
